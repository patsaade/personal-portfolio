# Vercel's analytics/speed-insights packages declare an optional @sveltejs/kit
# peer that pulls Vite 8; this project runs Vite 7 (via Astro 6). That optional
# peer is irrelevant here, so relax strict peer resolution to avoid false conflicts
# (locally and in Vercel's CI install).
legacy-peer-deps=true
